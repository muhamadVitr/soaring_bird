import 'package:flutter/material.dart';

const kSearchHighlight =
    TextStyle(fontFamily: 'Amiri', color: Colors.deepOrange, fontSize: 16);
const kSearchResults =
    TextStyle(fontFamily: 'Amiri', color: Colors.black, fontSize: 16);

const kIconSearchLight = Icon(Icons.search, color: Colors.grey);
const kIconCloseLight = Icon(
  Icons.close,
  color: Colors.grey,
);
const kIconBackLight = Icon(Icons.arrow_back, color: Colors.grey);

// const kSearchResults =
