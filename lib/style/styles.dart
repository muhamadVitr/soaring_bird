import 'package:flutter/material.dart';

const kSearchHighlight = TextStyle(
    fontFamily: 'EBGaramond', color: Colors.deepOrange, fontSize: 16.5);
const kSearchResults =
    TextStyle(fontFamily: 'EBGaramond', color: Colors.black, fontSize: 16.5);
const kMainText =
    TextStyle(fontFamily: 'EBGaramond', color: Colors.black, fontSize: 17);

const kSuperText = TextStyle(color: Colors.grey, fontSize: 12);

const kIconSearchLight = Icon(Icons.search, color: Colors.grey);
const kIconCloseLight = Icon(Icons.close, color: Colors.grey);
const kIconBackLight = Icon(Icons.arrow_back, color: Colors.grey);

// const kSearchResults =
